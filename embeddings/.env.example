# Copy this file to .env and adjust if needed.
# `docker compose up -d` reads .env automatically.

POSTGRES_USER=openclip
POSTGRES_PASSWORD=openclip
POSTGRES_DB=openclip
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
